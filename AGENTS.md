# Traffic Control Pilot agent instructions

Read README.md, docs/protocol.md and BACKLOG.md before work. Preserve this repository's deterministic governance-protocol role: AgentFlow remains the executor and runtime authority; Proofloom owns governed design intent and outcome audit; structural alternatives are proposals, not self-authorizing execution.

## Prepared continuation

For a later explicit operator `begin` on the prepared integration initiative, follow docs/BEGIN-INTEGRATION.md and its shared Proofloom work directive. Begin with current branch, authority and contract inspection. Resolve native governed-handoff v2 parity before enabling the existing TCP-002/TCP-003 AgentFlow observation/intervention work. Reuse TCP-006 for the return audit.

The BACKLOG is a planning projection, not authority to write another repository. Bind exact local and target-repository scopes through their native adoption/approval paths. Do not fabricate approvals or runtime evidence, turn a synthetic demo into a live-integration claim, install a second scheduler or silently accept v1 as a v2 execution fallback.

Preparation starts no implementation. `begin` is a natural-language instruction, not a new CLI command or human-authentication record. Preserve unpublished work and protected tests; do not automatically merge, deploy, mutate providers or consumers, rotate credentials, force-push or delete remote branches. Stop on genuine authority, scope, execution-access or validation blockers; do not reopen the already selected architecture.

Run the existing repository checks for actual code changes: npm test, npm run demo and npm run demo:json. Record the exact revision and actual results. Do not claim checks ran merely because this instruction file exists.
